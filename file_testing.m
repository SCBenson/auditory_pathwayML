binsize=1e-3; % 1ms bin size
duration=0.7; % 700ms stim duration

% Add the neccessary scripts to the path
savepath=path;
addpath(genpath(pwd));

% Path to AN data
channelChoice=fullfile('Data','AN','natural');
fileList = dir(datadir); % gains the list of .spk files for 'natural'.
lenFile = length(fileList);
neurogramsList = zeros(3, 16, 10);

for i = 3:lenFile

end
    
    
    

